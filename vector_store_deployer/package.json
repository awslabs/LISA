{
  "name": "vector_store_deployer",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "npm run clean:dist && node esbuild.js && npm run copy-assets && npm run copy-dist",
    "copy-assets": "cp package.json ./src/cdk*json ../VERSION ./dist",
    "copy-dist": "mkdir -p ../dist/vector_store_deployer && cp -r ./dist/* ../dist/vector_store_deployer/",
    "clean": "rm -rf ./dist ./node_modules",
    "clean:dist": "rm -rf ./dist",
    "test": "echo \"No tests for vector store deployer package\""
  },
  "author": "",
  "license": "Apache-2.0",
  "dependencies": {},
  "devDependencies": {
    "@cdklabs/cdk-enterprise-iac": "^0.1.0",
    "@types/aws-lambda": "8.10.160",
    "@types/node": "^25.0.10",
    "aws-cdk": "^2.1033.0",
    "aws-cdk-lib": "^2.236.0",
    "esbuild": "^0.27.1",
    "zod": "^4.1.13"
  }
}
