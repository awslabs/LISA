# AWS SDK Dependencies
# Keep boto3 aligned with LiteLLM / RDS IAM token refresh expectations.
# litellm[proxy]==1.83.12 pins boto3==1.42.59
boto3>=1.43.30,<1.44.0

# OpenTelemetry - Optional for LiteLLM Weave integration, silences import warnings
opentelemetry-api>=1.42.1
opentelemetry-sdk>=1.42.1

aiohttp>=3.14.1,<4.0.0
backoff>=2.2.1,<3.0.0
cachetools>=7.1.4,<8.0.0
click>=8.4.1,<9.0.0
cryptography>=49.0.0,<50.0.0
fastapi==0.137.1
gunicorn>=26.0.0,<27.0.0

# LiteLLM — pinned by policy (orchestration / proxy compatibility).
# Fixes: "All connection attempts failed" errors every 15 minutes with IAM auth (older LiteLLM).
# 1.83.8+ caps Python below 3.14; use 1.83.7 when building/running on Python 3.14+ until LiteLLM supports it.
litellm[proxy]==1.83.12; python_version < "3.14"
litellm[proxy]==1.83.7; python_version >= "3.14"

loguru>=0.7.3,<0.8.0
pydantic>=2.13.4,<3.0.0
PyJWT>=2.13.0,<3.0.0
prisma>=0.15.0,<0.16.0
starlette>=1.3.1,<1.4.0

# ASGI Server — litellm[proxy] extra requires uvicorn>=0.49.0,<1.0.0
uvicorn>=0.49.0,<1.0.0
