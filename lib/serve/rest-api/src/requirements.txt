# AWS SDK Dependencies
# Keep boto3 aligned with LiteLLM / RDS IAM token refresh expectations.
# litellm[proxy]==1.100.1 pins boto3==1.43.1
boto3>=1.43.93,<1.44.0

# OpenTelemetry - Optional for LiteLLM Weave integration, silences import warnings
opentelemetry-api>=1.44.0
opentelemetry-sdk>=1.44.0

aiohttp>=3.14.3,<4.0.0
backoff>=2.2.1,<3.0.0
cachetools>=7.1.8,<8.0.0
click>=8.5.0,<9.0.0
cryptography>=50.0.1,<51.0.0
fastapi==0.141.1
gunicorn>=26.2.0,<27.0.0

# LiteLLM — pinned by policy (orchestration / proxy compatibility).
# >=1.86.2 fixes GHSA-4xpc-pv4p-pm3w (host-header auth bypass). Requires Python <3.14.
litellm[proxy]==1.100.1; python_version < "3.14"

loguru>=0.7.3,<0.8.0
pydantic>=2.13.5,<3.0.0
PyJWT>=2.14.0,<3.0.0
prisma>=0.15.0,<0.16.0
starlette>=1.6.0,<1.7.0

# ASGI Server — litellm[proxy] extra requires uvicorn>=0.52.4,<1.0.0
uvicorn>=0.52.4,<1.0.0
