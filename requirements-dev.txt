# Dev/test environment dependencies
# Runtime deps are managed by each module's own requirements.txt/pyproject.toml
# This file only contains: test frameworks, dev tools, and deps needed
# to import all modules into a single test venv.
#
# Python 3.14: litellm[proxy] forces orjson 3.10.15 (no usable cp314 build). Use
#   bash scripts/install-python.sh
# which installs litellm without deps, backfills proxy requirements except orjson, then adds orjson from a wheel.

# LiteLLM - needed to run rest-api tests in the shared venv.
# 1.83.8+ requires Python <3.14; on 3.14+ pip installs 1.83.7 instead.
litellm[proxy]==1.83.12; python_version < "3.14"
litellm[proxy]==1.83.7; python_version >= "3.14"

# Test frameworks
pytest==9.0.3
pytest-cov==7.1.0
pytest-xdist==3.8.0
pytest-asyncio==1.3.0
coverage>=7.14.0
responses>=0.26.0
moto==5.2.1

# Test data / evaluation
pyarrow>=24.0.0
datasets==4.8.5

# Langchain - needed by tests that import langchain directly
langchain==1.3.1
langchain-openai==1.2.1

# Lambda runtime deps - needed because tests import lambda code which
# has top-level imports of these packages
mangum==0.21.0
python-docx==1.2.0
opensearch-py==3.2.0
pypdf==6.11.0
lxml==6.1.1
psycopg2-binary==2.9.12
requests-aws4auth==1.3.2
langchain-community==0.4.1
langchain-core==1.4.0
langchain-text-splitters==1.1.2
cachetools==7.1.3
tiktoken==0.13.0
fastapi==0.136.1
pydantic==2.13.4
# litellm[proxy] 1.83.7 (Python 3.14+ fallback) pins cryptography==48.0.0 for the proxy extra.
cryptography==46.0.7; python_version < "3.14"
cryptography==48.0.0; python_version >= "3.14"
starlette>=1.0.0
loguru==0.7.3
fastapi_utils==0.8.0

# Linting / formatting / type checking
black==26.5.1
flake8==7.3.0
mypy==2.1.0
pre-commit==4.6.0

# Build tools
poetry==2.4.1

--only-binary=pyarrow,lxml,psycopg2-binary
