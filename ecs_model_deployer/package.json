{
  "name": "ecs_model_deployer",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "tsc && npm run copy-dist && npm run pack:prod",
    "copy-dist": "cp Dockerfile package.json ./src/cdk*json ../VERSION ./dist",
    "pack:prod": "cd ./dist && npm i --omit dev",
    "clean": "rm -rf ./dist/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "Apache-2.0",
  "dependencies": {
    "@cdklabs/cdk-enterprise-iac": "^0.0.512",
    "aws-cdk": "^2.177.0",
    "aws-cdk-lib": "^2.185.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^22.4.1"
  }
}
